export const saveSettings = (label: string, activeAlarm: string, volume: number | number[]) => {
    console.log('label', label); //working
    console.log('alarm', activeAlarm)
    console.log('volume', volume);
}